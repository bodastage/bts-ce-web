let initialState = {
    requestingData: false,
    requestError: null,
    data: [],
    fields: []
};


export default function baseline(state = initialState, action) {
        
        return state;
}
